import 'package:flutter/material.dart';

class RemainingDealsScreen extends StatelessWidget {
  const RemainingDealsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
